Para leitura de documentação, pode-se fazer o seguinte:
    - Abrir o interpretador do python: basta digitar "python" no terminal,
     que o interpretador abrirá.
    - Buscar a documentação de algo, por exemplo: "import pandas as pd" e
     "help(pd.read_csv)". Ao fazer isto, a documentação daquele método
     aparecerá.

Diferença de método e propriedade:
    - método -> precisa de parênteses: df.head(10)
    - propriedade -> sem parâmetros: df.dtypes

pd.read_csv(filepath, sep=',')
    - É um método do pandas (com parênteses).
    ARGUMENTOS
    - filepath: caminho para o arquivo CSV (string)
    - sep: separador de colunas (padrão é vírgula)
    RETORNO:
    - DataFrame

df.head() / df.tail() / df.to_string(): Maneiras eficientes de mostrar o DF.

df.dtypes
    - Propriedade que mostra os tipos e os nomes das colunas de um DF.

df.select_dtypes(include=None, exclude=None)
    - É um método do pandas (com parênteses).
    ARGUMENTOS
    - include: tipo(s) de coluna para selecionar.
    - exclude: tipo(s) de coluna para ignorar.
    RETORNO:
    - DataFrame só com as colunas do tipo escolhido.

Numpy Array (1D): Equivale apenas a uma lista crua de dados, por exemplo
[0, 1, 0, 2, ...]. Não tem nome, não tem formato de tabela, só texto solto.

Pandas Series (1D): É uma única colina do Excel, Ela tem os dados e um título
no topo.

Pandas DataFrame (2D): É a planilha inteira, é feita de várias "Series"
coladas lado a lado.

pd.DataFrame():
    - 1° Parâmetro: "dados", precisa ser 2D.
    - 2° Parâmetro: "columns", parâmetro que mostra quais serão as colunas
        que serão criadas para o DataFrame.

ohe_encoder.get_feature_names_out():
    Método do OneHotEncoder, que consegue ver o nome das colunas, pode ser
    utilizado em conjunto com o parâmetro "columns" de pd.DataFrame() para
    facilitar a codificação dos dados categóricos.

-------------------------------------------------------------------------------------------
LabelEncoder (SKLearn): Converte categorias em números inteiros sequenciais:
    - Iris-setosa       -> 0
    - Iris-versicolor   -> 1
    - Iris-virginica    -> 2

    Uma coluna vira uma coluna, O problema: o modelo pode interpretar
    `2 > 1 > 0`, ou seja, que estes números são ORDINAIS (existe ordem
    entre eles), o que não seria verdadeiro se tratando de espécies de
    flores.

OneHotEncoder (SKLearn): Resolve o problema de dados ordinais criando
 uma coluna binária para cada categoria, assim gerando os dados NOMINAIS:
    - Iris-setosa       -> [1, 0, 0]
    - Iris-versicolor   -> [0, 1, 0]
    - Iris-virginica    -> [0, 0, 1]

    Cada coluna vira várias colunas. Não implica ordem e é o mais correto
    para dados categóricos nominais.

pd.get_dummies() (Pandas): Faz exatamente a mesma coisa que o OneHotEncoder,
 porém ele é um método do pandas (mais simples de usar), retornará um DataFrame
 direto. A diferença prática é que o OneHotEncoder pode ser salvo com o Pickle
 e reutilizado em dados novos. O get_dummies() não tem esse "estado" para salvar.

RESUMO:
- LabelEncoder (SKLearn)    ->  1 coluna -> 1 coluna numérica (implica ordem).
- OneHotEncoder (SKLearn)   ->  1 coluna -> N colunas binárias (não implica ordem).
- pd.get_dummies() (Pandas) ->  1 coluna -> N colunas binárias (não implica ordem).

Para dados numéricos, é necessário normalizar (ou padronizar) os dados utilizando
algum método scaler, como o MinMaxScaler() do SKLearn.

Para dados categóricos, é necessário codificar os dados, utilizando algum encoder,
como LabelEncoder() ou OneHotEncoder(), ambos vistos acima.
-------------------------------------------------------------------------------------------

Opções para o tratamento de valores ausentes (NA)
1. Exclusão de Dados (Drop)
    - Remover Linhas (Listwise deletion / dropna):
        > Quando usar: quando a quantidade de NAs é mínima (geralmente <5% do dataset) e
         a remoção não altera a representatividade da amostra.
        > Contra: Risco de perder muita informação útil se aplicado em larga escala.
    
    - Remover Colunas:
        > Quando usar: quando uam coluna possui taxa massiva de NAs (ex: > 40-50%)
         e não possui forte correlação com a variável alvo.

2. Imputação Simples (Preenchimento Estático)
    - Mediana: Preenche com o valor central:
        > Quando usar: varuáveis numéricas com muitos outliers (valores extremos),
         pois a mediana é resistente a distorções.
        
    - Média: Preenche com a média aritmética:
        > Quando usar: variáveis numéricas com distribuição normal/simétrica
         (sem outliers graves).
    
    - Moda: Preenche com o valor mais frequente:
        > Quando usar: variáveis categóricas (texto/classes) ou numéricas discretas.
    
    - Valor Constante (fillna): substituir por "Desconhecido", "0", ou "-1":
        > Quanod usar: quando a própria ausência da informação é um dado importante
         (ex: se o campo "Data de Cancelamento" é NA, o cliente ainda está ativo).

3. Imputação em Séries Temporais (Time Series)
    - Forward Fill (ffill) / Backward Fill (bfill): propaga o último falor válido
     para frente ou o próximo valor para trás.
    
    - Interpolação: traça uma reta ou curva entre o ponto anterior e o próximo
     para estimar o valor intermediário.
        > Quando usar: sensores, cotações financeiras, e dados onde existe uma
         continuidade temporal óbvia.

4. Imputação Avançada (Machine Learning)
    - KNN Imputer: encontra os k vizinhos mais próximos (linhas mais parecidas com
     a que tem o NA) e tira a média/mediana deles para preencher o buraco.
    
    - Interative Imputer (Multivariate): trata a coluna com NAs como a variável alvo
     e usa as outras colunas como features para treinar um modelo de regressão e
     prever o valor exato.
        > Quando usar: quando a precisão é fundamental e você tem poder computacional
         disponível.

5. Resumo - Para dados:
    Categóricos - Preencher com a MODA.
    Numéricos (outliers grandes) - Preencher com a MEDIANA.
    Numéricos (sem outliers significativos) - Preencher com a MÉDIA.
    Séries Temporais/Sequenciais: Propagação (ffill/bfill) ou Interpolação.
    Ausência é Informação: Preenchimento estático (ex: "Desconhecido" ou "0").